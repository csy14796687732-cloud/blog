export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum PostStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
}

export enum CommentStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
