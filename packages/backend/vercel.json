{
  "installCommand": "npm install",
  "buildCommand": "echo done",
  "outputDirectory": ".",
  "functions": {
    "api/index.ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
