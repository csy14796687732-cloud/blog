{
  "installCommand": "cd ../.. && pnpm install --filter=@blog/backend...",
  "buildCommand": "cd ../.. && pnpm --filter @blog/shared build && pnpm --filter @blog/backend build",
  "outputDirectory": ".",
  "functions": {
    "api/index.ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
